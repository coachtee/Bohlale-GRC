# Changelog

All notable changes to Bohlale GRC are documented in this file.

The format is loosely based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- Repository scaffolding: `BOHLALE_GRC_MASTER_SPEC.md`, `BUILD_STATUS.md`, `README.md`, `CHANGELOG.md`, `.env.example`.
- Django project scaffold (`config`) with 23 modular apps per the spec's suggested module list.

Further entries are appended chronologically below as modules are completed. See `BUILD_STATUS.md` for the current in-progress state of the build.
